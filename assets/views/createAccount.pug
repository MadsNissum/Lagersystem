doctype html
html(lang="en")
    head 
        title Lagersystem - Produkter  
        link(rel="stylesheet", href="/styles/style.css")
    body
        include header
        if showAlert
            div(class="alert")
                alert
                    span.closebtn(onclick="this.parentElement.parentElement.style.display='none';") &times;
                    strong Alert: Username is already taken! 🚨 

        div(class="loginContainer")
            div(class="loginMain")
                form(action="/postCreateAccount", method="POST")
                    label Username 
                    br
                    br
                    input(type="text", name="username")
                    br
                    br
                    label Password 
                    br
                    br
                    input(type="password", name="password")
                    br
                    br
                    button(type="submit") Create
                    br
                    br
                    a(href="/login") Sign in
        include footer